1. Переименовать кнопку "Добавить заявку" в "Создать заявку"
2. в UC-REQ-02.3 - Поиск техники для добавления в заявку, в шаге 2 (Frontend загружает базовые справочники фильтров) - прописать, какие api справочники должны быть вызваны
3. В шаге 5 тоже прописать, какой справочник
4. Как бы выглядел поиск по динамическим параметрам? например мне бы хотелось найти технику, у которой объем двигателя выше 1,6 (показать наглядно без изменения файлов)
5. Принимает ли POST /booking-requests/{id}/items массив? если например пользователь выбрал не 1 тачку, а несколько? (что лучше батч или каждый запрос отдельно? без изменения файлов)
6. В ответе POST /booking-requests/{id}/items возвращаются только брони созданные в этом добавлени, или все брони по этой заявке? (без изменений в файлах)



---

Описать явно, что такое доступность:

POST /booking-requests/{id}/items

Для каждого элемента проверить доступность на выбранный период.

EquipmentStatuses - нет активных записей на период брони

---

POST /booking-requests/{id}/items

пока поле jdeWorkOrderStepRefId можно опустить, идем по самому базовому сценарию.

---

В UC-REQ-02.4 - Добавление техники в draft-заявку нужно добавить:
что после выполнения POST /booking-requests/{id}/items frontend должен отобазить пользователю страницу "Новая заявка / Редактировать заявку" с номером заявки
В сайд-баре все также:
- флажок default work order-a
- номер WO
- Приоритет
- Локация
- Описание работ
- Комментарий

В центральной части:
общее количество добавленной техники (броней) (не только по последнему вызову API)
Список броней с полями:
- Фото техники
- Тип техники
- Марка, модель
- Номер ТШО
- ГРНЗ
- Данные Fleet owner
- Даты начала, завершения брони (плановые)
- Характеристики техники (ключ - значение)
- Поле justification (именно тут пользователь должен его ввести)

По сути это обновленное состояние страницы "Новая заявка", только с уже добавленными бронями

---

в таблице Equipments поле - plannedEngineHoursPerDay - Переделай на plannedMotohourPerDay

EquipmentStatuses - Добавить поле для иконки

startDt, endDt - дай более осмысленные названия полей

после обновления - укажи в начале документа, что поменялось
проверь API, есть ли влияние на них по этим полям?