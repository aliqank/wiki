1. Первое, что нужно сделать:
Нужно понять, что нужно для того, чтобы реализовать страницу букинга со стороны реквестора:
И так, у нас есть пустая стр. со стороны реквестора (пустая таблица, потому что заявок еще нет).

И так для начала нам нужен API, чтобы получать список текущих заявок. Для этого нам нужен будет API:
GET /booking-requests/my
так, вижу, что нужно дополнить query по полям:
- дата подачи заявки (диапазон дат)

так, теперь по ответу:
Вот так выглядит ответ:
```
{
  "value": {
    "items": [
      {
        "id": "c777f75f-029d-4d8f-8c69-e74a1d280001",
        "requestNumber": "REQ-2026-00015",
        "type": "Regular",
        "status": "Submitted",
        "priority": "P2",
        "workOrderJdeId": "WO-10025",
        "createdAt": "2026-05-14T09:15:00Z"
      }
    ],
    "total": 1
  },
  "isSuccess": true,
  "errors": []
}
```

в интерфейсе мы ожидаем:
- номер заявки
- дата создания
- данные реквестора
- номер ворк ордера
- приоритет
- список броней
    - тип техники
    - марка модель
    - номер ТШО
    - ГРНЗ
    - ворк центр
    - владелец
    - диапазон брони (план)
    - диапазон брони (факт)
    - статус
    - статус 

---

Интересно, как лучше: 
- тут в ответе передавать только список id броней, а потом отдельно тянуть сами брони? или сразу внутри объекта списка передавать все данные брони?