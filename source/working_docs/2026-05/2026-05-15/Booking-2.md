Окей, мы определили, что по UC-REQ-02 пользователь нажимает на "добавить заявку"
открывется окно
в этот момент вызывается "POST /booking-requests"
то есть публикуется пустая заявка, чтобы создать драфт

---

Теперь. тут важно пока забыть о заявках, которые приходят автоматически, или если не забыть, то правильно с ними работать:
давай пока представим, что пользователь просто хочет создать заявку, он еще не знает о jde заявках

в BookingRequests переименовать workOrderJdeId в workOrderNumber, и все места где он указан, тоже переименовать.

---

теперь пользователь например создает заявку без привязки в jde.
тогда он юзает POST /booking-requests заходя в страницу.
API нужно подредактировать: workDescription, priority не могут быть обязательными, если запрос отправляется как только пользователь зашел.

--- 

окей, теперь пользователь заполняет поля:
если он поставил флажок "default work order"
тогда поле номер Work Order должно заблокироваться, и там должно отображаться 0000001, которое позже пойдет в запрос, отдельно флаг никуда передавать не надо.

выставляет приоритет, прописывает локацию, описание работ, коммент

---

теперь тут система предлагает патчить изменения после заполнения, а что будет триггером для патча?

---

окей представим, что пока мы решили эту проблему.
далее пользователь нажимает "добавить технику"
система вызывает GET /equipment/search

для начала система просто отображает рандомный список техники, так как пользователь еще не применил фильтров

---

должен ли фронт подтянуть справочники на фронт, чтобы потом передать в GET /equipment/search? если да, то какие

---

пользователь выставил фильтры, выбрал пару ед. техники. При каждом выставлении нового фильтра, фронт должен досылать GET /equipment/search
а по кнопке сбросса - вернуться в рандом.

---

пользователь нажимает "добавить технику"

---

система делает POST /booking-requests/{id}/items

--- 

и только потом при необходимости заполняет justification при нажатии "отправить заявку" (POST /booking-requests/{id}/submit)

---

как эти комменты повлияют на UC 2?

