# Контекст сессии — 2026-04-20

## Что делали
- Пользователь открыл файл drawio-схемы БД (`Booking tool.drawio.xml`) и попросил проанализировать её на предмет пробелов
- Параллельно был прочитан BRD v7 (`2026-04-20 - HDV HDE BRD v7.md`) как источник требований
- Выполнен gap-анализ: сравнение текущих таблиц/полей схемы с требованиями BRD v7
- Составлен детальный документ с предложениями по дополнению схемы

## Принятые решения
- Анализ схемы выполнен полностью; результат сохранён в "Результаты claude/"
- В ходе анализа выявлена архитектурная проблема: `FleetOwnerId` в таблице `Fleet` — единственный FK, хотя по BRD один флот может иметь 2+ Fleet Owner → нужна M2M таблица `FleetFleetOwner`
- Таблица в схеме названа `EquipmentId` — рекомендовано переименовать в `Equipment`

## Созданные / изменённые файлы
- `Результаты claude/2026-04-20 - Анализ схемы БД drawio - пробелы и дополнения.md` — основной результат сессии; детальный gap-анализ с предложениями

## Открытые вопросы и TBD
- Поле `CargoBookingItemId` в таблице `Bookings` — неясно, это FK на отдельную таблицу или внешний идентификатор; требует уточнения у разработчиков
- Таблица `WorkOrders` (кэш JDE WO) — нужно уточнить: хранить весь WO или только нужные для формы создания Request поля
- Справочники (handbook tables) — нужно решить: 10 отдельных таблиц или иерархическая структура (одна таблица с типом)?
- `AuthorizedRequestors` — уточнить гранулярность: авторизация на уровне конкретной единицы или на уровне флота?
- OQ-NEW-K (route tracking на GIS-карте) — решение ожидалось 20.04; если принято — нужна таблица `EquipmentRoutes`

## Следующие шаги
- Обновить drawio-схему согласно выявленным пробелам (критичные поля в `BookingRequests`, `Equipment`, `Fleet`)
- Исправить архитектуру `Fleet ↔ FleetOwner`: добавить M2M таблицу `FleetFleetOwner`
- Добавить в схему новые таблицы: `EquipmentTrackers`, `EquipmentPhotos`, `EquipmentRepairs`, `WorkOrders`
- Добавить в схему справочные таблицы (handbook/reference)

## Дополнительный контекст
- Схема была только что создана (дата файла 2026-04-20) — это ранняя версия, большинство таблиц кроме `BookingRequests` и `Bookings` являются заготовками
- BRD v7 — актуальная версия требований на 2026-04-20; внесены изменения по утилизации (три метрики, дашборд), GIS-карте, Default WO/WC для Logistics, истории заявок Requestor
- Таблицы `BookingRequests` и `Bookings` проработаны достаточно хорошо в схеме; основные пробелы — в `Equipment`, `Fleet`, `FleetOwners` и отсутствующих таблицах
- Для дашборда утилизации критичны: `EquipmentRepairs` (ремонтные дни из JDE), поля `TargetEngineHoursPerDay` / `TargetKmPerDay` в `Equipment`, трекеры в `EquipmentTrackers`
- Поле `DepartmentId` должно быть в `Equipment`, НЕ в `FleetOwners` — это закрытый вопрос OQ-NEW-J (17.04)
